# IAI — Test Outputs

Outputs from inter-agent infection tests (payload relayed through pipeline).

**Naming:** `{payload-id}-{target}-{date}.md`  
Add `-safety` suffix for runs with safety prompt active.
